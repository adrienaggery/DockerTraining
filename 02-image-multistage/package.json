{
  "name": "02-image-server-web",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "start": "tsc && node dist/server.js",
    "build": "tsc && cp -r src/public dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrien Aggery",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
